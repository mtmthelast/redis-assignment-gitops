apiVersion: v1
kind: Service

metadata:
  name: python-api

spec:
  selector:
    app: python-api

  type: {{ .Values.service.type }}

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}